name: Deploy Backend (BE Only)
on:
  push:
    branches: ['main']
    paths: ['be/**']
  workflow_dispatch:

jobs:
  deploy-be:
    name: ⚙️ Deploy BE
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get code
      uses: actions/checkout@v4
    
    - name: 📂 Sync BE to Host
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        
        # Lấy file TỪ folder be
        local-dir: be/
        
        # Đẩy VÀO folder htdocs
        server-dir: /htdocs/
        
        # --- QUAN TRỌNG NHẤT: DÒNG NÀY CẤM XÓA FILE CỦA FRONTEND ---
        delete: "false"
        # -----------------------------------------------------------
        
        exclude: |
          **/.git*
          **/.git/**